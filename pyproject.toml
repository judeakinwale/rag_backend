[project]
name = "rag-backend"
version = "0.1.0"
description = "RAG backend services"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/rag_packages",
    "services/user-service",
]
